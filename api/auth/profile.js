import { cookie, validSession, readSecure, writeSecure } from '../_secureStore.js'
export default function handler(req,res){const s=validSession(cookie(req,'hiper_session'));if(!s)return res.status(401).json({error:'Não autenticado'});const users=readSecure('users',[]);const i=users.findIndex(x=>x.email===s.email);if(i<0)return res.status(404).json({error:'Usuário não encontrado'});if(req.method==='GET')return res.json({name:users[i].name||'',email:users[i].email,phone:users[i].phone||''});if(req.method!=='PUT')return res.status(405).json({error:'Método não permitido'});users[i]={...users[i],name:String(req.body?.name||'').slice(0,100),phone:String(req.body?.phone||'').slice(0,30)};writeSecure('users',users);return res.json({saved:true,name:users[i].name,email:users[i].email,phone:users[i].phone})}
