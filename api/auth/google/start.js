export default function handler(req,res){const client=process.env.GOOGLE_CLIENT_ID;const origin=process.env.PUBLIC_APP_URL||`${req.headers['x-forwarded-proto']||'http'}://${req.headers.host}`;if(!client)return res.status(503).json({error:'Configure GOOGLE_CLIENT_ID e GOOGLE_CLIENT_SECRET para ativar o Google.'});const params=new URLSearchParams({client_id:client,redirect_uri:`${origin}/api/auth/google/callback`,response_type:'code',scope:'openid email profile',access_type:'offline',prompt:'select_account'});res.redirect(`https://accounts.google.com/o/oauth2/v2/auth?${params}`)}
